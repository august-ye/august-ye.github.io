\documentclass{article}

\usepackage[margin=1in]{geometry}
\linespread{1.2}
\usepackage{amsmath,amsthm,amssymb,amsfonts}

 
\title{Real Analysis I --- Homework 1}

% Replace with your name and ID below
\author{FirstName LastName \\ ID \#: 123-45-6789}

\date{}


\begin{document}

\maketitle

\begin{enumerate}

\item Let $\mathcal{F} = \{(a,b) \subset \mathbb{R}: a, b \in \mathbb{Q}, a<b\}$. Is $|\mathcal{F}| = \aleph_{0}$? If yes, find a one-to-one correspondence between $\mathcal{F}$ and $\mathbb{N}$. If not, prove your claim.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% Hint: list rational numbers as rk and then each interval as A_{kj}.


%\newpage % uncomment this line to start on a new page

\item Suppose $E \subset \mathbb{R}$, and $|x-y|>1$ for any $x,y\in E$. Prove that $E$ is at most countable.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% hint: for each x in E, the interval (x-1/3,x+1/3) only contains x from E.


%\newpage % uncomment this line to start on a new page

\item Suppose that $A, B, C$ are sets, $A \subset B$, and $A \sim A \cup C$. Prove that $B \sim B\cup C$.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% A \subset A \cup (C\setminus B) \subset A \cup C, which by Bernstein it implies A \sim A \cup (C\setminus B). on the other hand B\minus A \sim B\minus A, take union of the two sides for these two gives the result.


%\newpage % uncomment this line to start on a new page

\item Suppose $A \subset B$. Show that $A' \subset B'$ and $\bar{A} \subset \bar{B}$.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Show that $\mathbb{R}\setminus \mathbb{Q}$ is a $G_{\delta}$-set.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% let Q = {rk}, P = R\Q. Let Gk = R\{rk} which is open, then P = \cap_{k}G_{k} which is G_{delta}


%\newpage % uncomment this line to start on a new page

\item Suppose $(X,d)$ is a metric space and $E \subset X$. Prove that the following three statements are equivalent:

\begin{enumerate}
\item $x_{0} \in E'$.
\item There exists a sequence $\{x_{k}\} \subset E$, $x_{k} \ne x_{0}$ for all $k\in\mathbb{N}$, such that $x_{k}\to x_{0}$.
\item For any open set $U$ such that $x_{0} \in U$ ($U$ is called an open neighborhood of $x_{0}$), $U$ contains infinitely many points of $E$.
\end{enumerate}

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $\{f_{j}\}$ is a sequence of functions where $f_{j}: \mathbb{R}^{n} \to \mathbb{R}$. Define $E_{j,k} = \{x \in \mathbb{R}^n: f_{j}(x) \ge \frac{1}{k}\}$ for $j,k\in \mathbb{N}$. Express the set $\{x \in \mathbb{R}^{n}: \limsup\limits_{j\to\infty} f_{j}(x) > 0\}$ in terms of $E_{j,k}$'s.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% \cup_{k}\cup_{J}\cap_{n} E_{kj}


%\newpage % uncomment this line to start on a new page

\item Suppose $\{A_{k}\}$ and $\{B_{k}\}$ are two sequences of sets. Prove that
\[
\limsup_{k\to\infty} (A_{k} \cup B_{k}) = \left(\limsup_{k\to\infty} A_{k} \right) \cup \left(\limsup_{k\to\infty} B_{k} \right)
\]

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% contains is trivial. For any x in LHS, x is in infinitely many of Ak\cap Bk, hence x in infinitely many Ak or infinitely many Bk

%\newpage % uncomment this line to start on a new page

\item Suppose $G_{1}$ and $G_{2}$ are open sets in $\mathbb{R}^{n}$, and $G_{1} \cap G_{2} = \emptyset$. Show that $G_{1} \cap \bar{G_{2}} = \emptyset$.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $f: \mathbb{R} \to \mathbb{R}$ is a monotonically non-decreasing function, and $E = \{x \in \mathbb{R}: f(x+ \epsilon) - f(x-\epsilon) > 0, \ \forall\, \epsilon>0\}$. Show that $E$ is a closed subset of $\mathbb{R}$.

%uncomment below and write your proof after it.
%\begin{proof}
%Write your proof here.
%\end{proof}
% NTS E^{c} is open. For any x \in E^{c}, there is eps>0 such that f(x-eps) = f(x+eps), which means f(x-eps) = f(y) = f(x+eps) for all y in (x-eps,x+eps).

%\newpage % uncomment this line to start on a new page

\end{enumerate}


\end{document}