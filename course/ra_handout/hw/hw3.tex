\documentclass{article}

\usepackage[margin=1in]{geometry}
\linespread{1.2}
\usepackage{amsmath,amsthm,amssymb,amsfonts}


\newcommand{\mto}{\stackrel{\mu}{\rightarrow}}
\newcommand{\Mcal}{\mathcal{M}}


 
\title{Real Analysis I --- Homework 3}

% Replace with your name and ID below
\author{FirstName LastName \\ ID: 123-45-6789}

\date{}


\begin{document}

\maketitle

Unless otherwise specified, all functions are defined on $E \subset \mathbb{R}^{n}$ where $E \in \mathcal{M}$.
\bigskip

For notation simplicity, a set such as $\{x \in E: f(x) > t\}$ is written as $\{f>t\}$ for short.

\begin{enumerate}

\item Prove that $f: E \to \mathbb{R}$ is measurable iff for any $a,b \in \mathbb{R}$ and $a<b$, the set $\{x \in E: a \le f(x) < b\} \in \mathcal{M}$.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $f: [a,b] \to \mathbb{R}$ is differentiable. Show that $f'$ is measurable.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $f,g : E \to \mathbb{R}$ are measurable, and $g\ne 0$ a.e.~$E$. Show that $f/g$ is measurable.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $\{f_{k}\}$ is a sequence of measurable functions. Show that both $\{x\in E: \lim_{k} f_{k}(x)\ \mbox{exists}\}$ and $\{x\in E: \lim_{k} f_{k}(x)\ \mbox{does not exist}\}$ are measurable sets. (We consider the limit exists even if $\lim_{k}f_{k}=\infty$ or $-\infty$.)

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $f:E \to \bar{\mathbb{R}}$ is measurable and finite a.e.~$E$, where $\mu(E) < \infty$. Show that for any $\delta > 0$, there exists a bounded measurable function $g: E \to \mathbb{R}$ such that $\mu( \{x \in E: f(x) \ne g(x)\} ) < \delta$.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page

\item Suppose $f_{k} \mto f$ on $E$ and $g$ is finite a.e.~$E$, where $\mu(E) < \infty$. Show that $f_{k}g \mto fg$ on $E$.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page


\item Suppose $f_{k} \mto f$ and $g_{k} \mto g$ on $E$ where $\mu( E)< \infty$, and $f,g$ are finite a.e.~$E$. Show that
\begin{enumerate}
\item $f_{k} + g_{k} \mto f + g$.
\item $f_{k} - g_{k} \mto f - g$.
\item $f_{k}^{2} \mto f^{2}$.
\item $f_{k}g_{k} \mto fg$.
\end{enumerate}

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}



%\newpage % uncomment this line to start on a new page


\item Suppose $f: E \to \mathbb{R}$ and $\phi: \mathbb{R} \to \mathbb{R}$. Show that if $f$ is measurable and $\phi$ is monotone (not necessarily strictly), then $\phi(f(x)): E \to \mathbb{R}$ is measurable.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}


%\newpage % uncomment this line to start on a new page


\item Suppose $f: E \to \bar{\mathbb{R}}$ is finite a.e.~$E$. Show that if for any $\delta > 0$, there exists a continuous function $g: E \to \mathbb{R}$ such that $\mu(\{ x\in E: f(x) \ne g(x)\}) < \delta$, then $f$ is measurable.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}



%\newpage % uncomment this line to start on a new page


\item Prove that the converse of Egorov theorem is true for any $E \in \mathcal{M}$ (i.e., $\mu(E) \in [0,\infty]$): if for any $\delta > 0$, there exists a measurable subset $E_{\delta} \subset E$ such that $\mu(E_{\delta}) < \delta$ and $f_{k} \rightrightarrows f$ on $E \setminus E_{\delta}$, then $f_{k} \to f$ a.e.~$E$.

%uncomment below and write your proof after it.
%\begin{proof}
%\end{proof}




\end{enumerate}


\end{document}